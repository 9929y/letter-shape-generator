# Product

## Register

product

## Users

Yanice(产品设计师)以及她分享作品的观众。使用场景:桌面浏览器里打开单文件工具,调参数生成粒子文字动画,用于 vibecoding 创作、社交分享(小红书)、以及导出代码嵌入其他项目。观众端场景:打开导出的组件,hover 看粒子聚合成文字的效果。

## Product Purpose

粒子造字机(Letter Shaper Studio):单文件 HTML 创作工具。输入文字(单字符到整句,含中文),数千个粒子按 seeded RNG 填充字形轮廓,持续颤动呼吸,并可在多段文字间炸开重组。支持循环播放、hover 显现、摄像头手部互动三种模式,可将当前参数烘焙导出为 Vanilla JS / React / Vue 组件。成功标准:调参即时反馈、60fps、导出代码开箱可用。

## Brand Personality

三个词:精密、克制、灵动。视觉语言对齐 Claude 的设计系统(暖象牙底、暖近黑文字、细发丝线、陶土橙重点色,靠背景色阶而不是阴影分层):界面退后,让 canvas 里的粒子动画成为唯一主角。UI 动效细腻但不抢戏。

唯一允许 UI 抢戏的地方:Hand 模式下摄像头未开时画布上的那颗按钮 —— 它是整个界面里唯一的饱和色块,而且摄像头一开就消失。

## Anti-references

- AI 紫色渐变 / 霓虹发光 / 赛博风(SKILL.md 明确记录:科技感色板被用户否决并回退过两次,不得再引入)
- 花哨的面板装饰、bounce/elastic 缓动
- 破坏 9 色默认色板与 #d6cfc5 画布底色的"审美改良"

## Design Principles

1. **Canvas 是主角,面板是仪器** — 面板 UI 的一切动效服务于操作反馈,绝不与粒子动画争夺注意力
2. **每个参数改动即时可见** — 滑杆拖动时 canvas 实时响应,结构性重建在释放时发生
3. **确定性优先** — 同一 seed 永远同一构图;工具行为可预测、可复现。seed 不必出现在界面上:它活在 state、撤销历史与导出的 cfg 里,那才是复现真正发生的地方
4. **克制的质感** — 控件的 hover/active/focus 反馈保持 120-300ms / ease-out 的时序;深度来自 Claude 的背景色阶(页面 → 面板 → 控件槽),不是玻璃和阴影
5. **导出即成品** — 导出代码不是示例,是可直接进生产的组件

## Deviations from SKILL.md (Yanice 明确授权,2026-08-27)

安装在 `02_agent_memory_and_skills/letter-shape-generator-tool-bycoraldesign/SKILL.md` 的规范仍是参数默认值与架构的权威,但以下几条已被 Yanice 本人明确覆盖,后续会话不要"修正"回去:

1. **预设色板可以存在。** SKILL.md 记录「3 套科技感色板与高饱和默认板被明确否决并回退两次,不要再提」。该禁令针对的是 agent 自行发起的审美改良;Yanice 于 2026-08-26 主动要求加入莫兰迪色系。现状:默认仍是 Classic 九色加权板,Morandi 作为可切换预设并存。科技感/霓虹/高饱和方向的禁令依然有效。
2. **参数面板移到右侧。** SKILL.md 写「面板在 LEFT 是刻意偏离右侧惯例,保持不变」。Yanice 于 2026-08-27 要求改到右侧,理由是对齐 Apple 自家工具的检查器惯例(Final Cut Inspector / Keynote 格式面板)并让画布占据视觉主位。
3. **产品更名为 Letter Shaper Studio。** Yanice 于 2026-08-27 要求。仓库名与 skill 来源名保持原样。
4. **改用 Claude 的设计系统。** Yanice 于 2026-08-27 要求:原来的 Apple 蓝(#0071e3)与 SF 系统字体跟画布上那套 pale / 复古的粒子色板不是一路人。现状:UI 外壳(不含画布内容)整体换成 Claude 的暖色令牌 —— 页面 #faf9f5 / #262624,文字 #141413 / #3d3d3a,重点色 #c96442(暗色 #d97757)。**注意一处刻意的偏离**:Claude 自己的珊瑚色配白字只有 3.9:1,过不了本文件的 4.5:1 底线,所以按钮上的字用的是暖近黑(4.73:1 / 5.90:1)而不是白色。药丸按钮形状按 Yanice 要求保留(Claude 自己用的是 8px 圆角矩形)。字体用开源近似字(DM Sans),JetBrains Mono 是 Claude 系统里唯一开源的一款,用在所有数字读数上。
5. **Text 输入框两侧的 chevron 删除。** Yanice 于 2026-08-27 要求。↑ / ↓ 键循环字符的能力保留,快捷键弹层里本来就写着。
6. **色板成套化,替掉逐色编辑。** Yanice 于 2026-08-27 要求。SKILL.md 记录「色板被否决两次」针对的是 agent 自行发起的改良;这次是 Yanice 本人提出「做不同的 color palette 而不是让用户一个一个选颜色」。现状:7 套 theme,**每套自带配好的背景色** —— 因为原来 palette 和 background 是两个互不相干的状态,分在两个可折叠分组里,这正是「Numerals 该更浅」「Morandi 背景要跟着变」做不出来的根因。Classic 仍是默认,九色与 #d6cfc5 一字未动。逐色编辑与自定义背景就放在 Colour 画廊正下方(第 10 条),没有删除。
7. **面板重构为 Looks 优先,并且真的删掉控件。** Yanice 于 2026-08-27 要求「选项太少一些」,并在下一轮明确:**不是收进抽屉,是没人会点的直接砍掉**。原来 73 个控件 / 18 根滑杆 / 7 个分组。现状:一级只有 Text / Mode / Play 加两条视觉画廊(Look、Colour),参数收进 Adjust,颜色收进 Colours。**滑杆从 18 根减到 6 根** —— 删掉的是两类:被合并滑杆覆盖的重复入口(min/max size、tremble 三项、三段时长),以及只由 Look 设定的参数(edge feather、opacity、rotation、stack density)。另外 Sequence 并进 Text(一行一个短语),Config JSON 导出页删掉(那个对象本来就印在另外三份代码里)。**参数一个都没从引擎里删**,Look 照常写、导出照常带,只是界面上不再摆出来 —— 这条守住了「导出即成品」。
   经 Yanice 明确保留:逐色编辑(9 个色点 + 权重 + 增删)。
   *第 10 条在此基础上又前进了一步:Look 改名 Shape 并只管形状,Colour 与逐色编辑升到一级,Density + Grain 并成 Scale(滑杆 6 → 5 根)。*
8. **Seed 输入框删除,只留 Shuffle。** Yanice 于 2026-08-27 问「seed 是什么」—— 产品主人要停下来问,就说明这个标签对用户是失败的。上一轮我拿设计原则 3 说服她留下它,那是问错了问题。现状:按钮叫 Shuffle(换一版构图),数字框删掉。原则 3 没有被破坏,见其条目。
9. **新增 Hand 模式(摄像头手部互动)。** Yanice 于 2026-08-27 要求:开摄像头后摊开手掌推散粒子、握拳抓取并拖动。识别是自己写的(肤色 + 运动 + 连通域 + 径向分布),不引入任何外部库或模型,「零依赖、离线可用、单文件」这条底线不破;精度换来的是可控与可导出——Hand 模式的导出代码里内联的就是同一个 `handTracker`。

10. **Shape / Colour 分成两条各画各轴的画廊,Scale 取代 Density + Grain,选中态改中性色,Export 移到面板底部。** Yanice 于 2026-08-28 逐条要求:
    - 「Look 跟 color 有点太像了」「look 它应该是这个的 shape,color 是 color」→ Shape 卡片用**单色油墨**把该预设的粒子真的画一遍(形状 / 尺寸分布 / 旋转都照做),Colour 卡片上**只有颜色**(自己的底色 + 按权重分宽的色条)。同时 **Look 不再改配色** —— 一个偷偷重画你颜色的预设,正是让这两个控件看起来是同一个东西的原因
    - 「用户也可以改里面的某一个颜色,或者添加 color palette,这些功能你都没有 enable」→ 九个色点与背景色从抽屉里拿出来,放在 Colour 画廊**正下方**;新增 **+ Save palette**(存当前颜色 + 背景,进 `localStorage`,刷新还在,hover 可删)。手改任一颜色,选中态掉成 Custom
    - 「Look 的这个 bar 还可以左右滑动,这不符合人体工学」→ 横向滚动整个删掉,两个画廊都是三列网格,**所有卡片一次全在屏幕上**,没有需要翻页的东西
    - 「选中的应该也不能是红色吧,应该是一个更 neutral 的 color」→ 选中态改成中性深色描边 + 对勾(`--select-ring`),红圈在一堆颜色上面读起来像报错。焦点环同时改成中性色
    - 「你要把右边改成一个跟左边的那个 size 一样大小的一个浮窗的 panel,而不是一个上下的这种结构」→ 面板不再贴着窗口右边缘,变成和画布等高的圆角浮窗卡片,四周留白;顶栏与状态栏去掉背板
    - 「score(Export)应该在下面」→ Export 从顶栏移到面板底部。移动端因为 sheet 是往下平移的,底部会被推出屏幕,所以在小屏上把它排到固定区正下方
    - 「Text 这边不好看」→ 一个居中的大写字母浮在一条宽灰槽里读起来像空字段;改成标签在上、左对齐、17px 的排版式输入框
    - **Scale 取代 Density + Grain**(Yanice 于 2026-08-28 提出:「用一个左右的滑杆去表示 scale,变小数量就变多,变大数量就变少」)→ `count = 36000 / size`,size 6–26px,数量 6000–1385。要用户去写粒子数,是把工具该做的算术推给了用户


## Accessibility & Inclusion

- 正文对比度 ≥ 4.5:1,双主题各自独立验证
- prefers-reduced-motion:UI 过渡压平;canvas 动画是内容本体且默认暂停,hover 模式降级为直接切换;Hand 模式是用户主动发起的实时互动,不做降级
- 摄像头只在用户点按后开启,离开 Hand 模式即关闭;每帧只进内存做识别,不上传、不留存
- 键盘可达:滑杆原生可键控、swatch 为真按钮、focus-visible 焦点环、空格播放/暂停
- 触控目标 ≥ 44px(小控件用扩大热区)
